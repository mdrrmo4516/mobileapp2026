# Example .env file
# Fill in with your Neon (or Postgres) connection string
# Example format:
# DATABASE_URL=postgres://username:password@<host>:5432/<database>

# If DATABASE_URL is not set during development, the app will automatically
# use a local SQLite database at ./dev.sqlite for convenience.
DATABASE_URL=
